import MenuContainer from '../MenuContainer/MenuContainer';

const RightMenu = () => {
  return (
    <MenuContainer>
      <div>Right Menu</div>
    </MenuContainer>
  );
};

export default RightMenu;
